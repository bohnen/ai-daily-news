# Copy to secrets.env (gitignored) and fill in. Never commit real keys.
# Nous Portal API key (OpenAI-compatible LLM)
LLM_API_KEY=
# TypeSafe API key (optional; triage is skipped when empty)
TYPESAFE_API_KEY=
